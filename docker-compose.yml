version: "3"

services:
    minecraft-server:
        container_name: minecraft-docker
        build:
            context: .
        volumes:
            - /mnt/modpack_server_data:/server
        ports:
            - 25565:25565
            - 25575:25575
volumes:
    modpack_server_data:



